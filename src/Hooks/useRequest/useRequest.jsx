import useAxios from "../useAxios/useAxios";


const useRequest = () => {
    const axiosLoader=useAxios();
    
    return (
        <div>
            
        </div>
    );
};

export default useRequest;